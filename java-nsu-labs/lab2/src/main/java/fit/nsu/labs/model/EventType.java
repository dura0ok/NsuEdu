package fit.nsu.labs.model;

public enum EventType {
    REDRAW_REQUEST,
    ALREADY_CLICKED,
    BOMB_OPENED,
    FLAG_STATE_UPDATE,
    REDRAW_TIMER,
    ALREADY_FLAGGED,
    RECORDS_ERROR,
    PRINT_MENU,
    USER_WIN
}
